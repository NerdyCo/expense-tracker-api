server:
  port: 8081
  
spring:
  application:
    name: expensetracker
  datasource:
    url: jdbc:postgresql://localhost:5432/expense-tracker
    username: postgres
    password: nerdyco
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:5050/realms/expense-realm
          jwk-set-uri: http://localhost:5050/realms/expense-realm/protocol/openid-connect/certs

keycloak:
  server-url: http://localhost:5050
  realm: expense-realm
  client-id: expense-tracker-admin
  client-secret: "UBtQnrk3q3vTeoYzIH9ozrn4URpHnoph"
  admin-username: admin
  admin-password: password123

logging:
  level:
    com.dwi.expensetracker.configs.SecurityConfig: DEBUG
    org.springframework.web.servlet.mvc: DEBUG
    org.keycloak: DEBUG
